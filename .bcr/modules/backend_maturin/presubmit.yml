matrix:
  bazel:
    - 8.x
    - 9.x
  platform:
    - debian11
    - ubuntu2204
    - macos_arm64
tasks:
  run_tests:
    name: Run maturin build tests
    bazel: ${{ bazel }}
    platform: ${{ platform }}
    test_targets:
      - //...
