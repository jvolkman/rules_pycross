[project]
name = "rules-pycross-test"
version = "0.1.0"
description = ""
requires-python = ">=3.9, <3.13"
dependencies = [
    "google-cloud-storage",
    "google-cloud-pubsub",
    "zope.interface",
    "zope.event",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
