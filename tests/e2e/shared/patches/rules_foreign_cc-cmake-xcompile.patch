--- a/foreign_cc/private/cmake_script.bzl
+++ b/foreign_cc/private/cmake_script.bzl
@@ -86,6 +86,11 @@ def _cmake_target_params(current_label, target_os, target_arch):
             params["CMAKE_SYSTEM_PROCESSOR"] = "armv7-a"
         else:
             params["CMAKE_SYSTEM_PROCESSOR"] = "armv7l"
+    elif target_arch == "aarch64" and target_os in ("macos", "ios", "tvos", "watchos"):
+        # Apple platforms use "arm64" (the Arm Ltd marketing name) rather than
+        # "aarch64" (the architecture's canonical name).  `uname -m` on macOS
+        # returns "arm64", and CMake's Darwin platform files expect that value.
+        params["CMAKE_SYSTEM_PROCESSOR"] = "arm64"
     elif target_arch in _CMAKE_SYSTEM_PROCESSOR:
         params["CMAKE_SYSTEM_PROCESSOR"] = _CMAKE_SYSTEM_PROCESSOR[target_arch]
     else:
@@ -278,8 +283,8 @@ _CMAKE_CACHE_ENTRIES_CROSSTOOL = {
     "CMAKE_RANLIB": struct(value = "CMAKE_RANLIB", replace = True),
     "CMAKE_SHARED_LINKER_FLAGS": struct(value = "CMAKE_SHARED_LINKER_FLAGS_INIT", replace = False),
     "CMAKE_STATIC_LINKER_FLAGS": struct(value = "CMAKE_STATIC_LINKER_FLAGS_INIT", replace = False),
-    "CMAKE_SYSTEM_NAME": struct(value = "CMAKE_SYSTEM_NAME", replace = False),
-    "CMAKE_SYSTEM_PROCESSOR": struct(value = "CMAKE_SYSTEM_PROCESSOR", replace = False),
+    "CMAKE_SYSTEM_NAME": struct(value = "CMAKE_SYSTEM_NAME", replace = True),
+    "CMAKE_SYSTEM_PROCESSOR": struct(value = "CMAKE_SYSTEM_PROCESSOR", replace = True),
 }
 
 def _create_crosstool_file_text(toolchain_dict, user_cache, user_env, target_os):
