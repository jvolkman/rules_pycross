[project]
name = "build-target-cross-repo-test"
version = "0.1.0"
description = ""
requires-python = ">=3.9, <3.15"
dependencies = [
    "six>=1.16.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
