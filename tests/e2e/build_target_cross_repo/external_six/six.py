"""Minimal six stub built by external_six for build_target e2e testing."""

__version__ = "1.17.0"

# Marker attribute to verify this wheel came from the external_six build_target
# rather than from PyPI. The real six module does not have this attribute.
BUILT_BY_EXTERNAL_REPO = True
