load("//shared:collect_wheels.bzl", "collect_wheels")

exports_files([
    "rpds_py.lock",
    "jiter.lock",
])

collect_wheels(
    name = "all_wheels",
    platforms = [
        "@llvm//platforms:linux_x86_64",
        "@llvm//platforms:macos_aarch64",
    ],
    wheels = [
        "@uv//rpds_py:wheel",
        "@uv//jiter:wheel",
        "@uv//python_geohash:wheel",
    ],
)
