load("@pycross_toolchains//:defs.bzl", "environments")

package(default_visibility = ["//visibility:public"])

exports_files([
    "pdm.lock",
    "poetry.lock",
    "pyproject.toml",
    "test_zstandard.py",
    "testlib.bzl",
])
