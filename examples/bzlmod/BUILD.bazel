load("@rules_python//python:defs.bzl", "py_binary")

py_binary(
    name = "ipython",
    srcs = ["ipython.py"],
    deps = [
        "@uv_deps//:amqp-mock",
        "@uv_deps//:ipython",
    ],
)

exports_files(["matplotlib.patch"])
