[project]
name = "rules-pycross-example-bzlmod"
version = "0.1.0"
dependencies = [
    "ipython>=8.17",
    "setuptools>=68",
    "wheel>=0.42",
    "amqp-mock==0.6.1",
    "matplotlib>=3.10.7",
]
requires-python = ">=3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
